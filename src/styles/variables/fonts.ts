import { Charm, Kanit } from "next/font/google";

export const charm = Charm({
  subsets: ["thai"],
  display: "swap",
  weight: ["400", "700"],
});

export const kanit = Kanit({
  subsets: ["thai"],
  display: "swap",
  weight: ["400", "600", "700"],
});
